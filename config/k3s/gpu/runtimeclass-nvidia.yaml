# RuntimeClass — tells Kubernetes to run these pods with the nvidia containerd
# runtime (configured on each GPU node by `nvidia-ctk runtime configure`).
# Apply from the K3s server:  kubectl apply -f config/k3s/gpu/runtimeclass-nvidia.yaml
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia

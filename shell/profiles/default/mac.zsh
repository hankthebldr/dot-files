# shell/profiles/default/mac.zsh
# macOS-specific aliases / hooks for the default profile.
# Currently empty — common.zsh covers the cross-platform set via platform.zsh shims.
# Add macOS-only integrations here if needed (e.g. open -a, osascript wrappers).

---
name: my-skill
description: >-
  Replace this with a one-line trigger written as "Use when …". This sentence
  is what the model reads to decide whether to load the skill, so make it
  concrete: name the task, the inputs, and the situation it applies to.
---

# My Skill

> Delete this template's guidance as you fill it in. Keep the final SKILL.md
> tight — anything long or reference-heavy goes in `references/` files that the
> model opens only when it needs them (progressive disclosure).

## When to use

- Bullet the concrete situations this skill handles.
- And the ones it explicitly does NOT — steer the model elsewhere.

## How it works

1. Step-by-step of what the model should do when this skill fires.
2. Reference helper files like `references/DETAIL.md` or `scripts/run.py`
   instead of inlining everything here.

## Examples

Show one worked example of input → action → output so the model can pattern-match.

<!--
Optional companion files (all relative to this skill dir):
  references/*.md   long-form detail loaded on demand
  scripts/*         executable helpers the skill can run
-->
